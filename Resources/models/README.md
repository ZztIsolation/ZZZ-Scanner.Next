# OCR Model

Release archives include `PP-OCRv5_mobile_rec_infer.onnx` in this directory.

The source repository keeps the ONNX model out of Git to make pushes and clones lighter. To run or publish from source, copy `PP-OCRv5_mobile_rec_infer.onnx` from a release archive into this folder.
